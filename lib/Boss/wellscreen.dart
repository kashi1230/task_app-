
import 'package:flutter/material.dart';

class WellScreen extends StatelessWidget {
  const WellScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child:Text("Well Screen"),
      ),
    );
  }
}
